# Directory UI Requirements

**Дата:** 2026-01-02  
**Статус:** Requirements Document

---

## 📋 UI/UX Принципы для Directory системы

### Компактность и единый стиль
- Следить за компактностью интерфейса
- Единый стиль во всех Directory страницах
- Использовать существующую дизайн-систему (Tailwind CSS v4)
- Следовать паттернам из Dashboard (белые карточки, shadow-sm, серые фоны)

### Современные, профессиональные решения
- Enterprise/travel CRM уровень качества
- Чистый, минималистичный дизайн
- Профессиональная типографика
- Понятная визуальная иерархия

### Минимизация кликов и шагов
- Быстрый ввод данных без лишних экранов
- Оптимизация под реальные рабочие сценарии
- Keyboard shortcuts для частых действий
- Автосохранение где возможно

### Friendly и интуитивный интерфейс
- Понятные labels и подсказки
- Валидация с понятными сообщениями
- Loading states и feedback
- Error handling с понятными сообщениями

### Использование существующих компонентов
- Использовать существующие UI паттерны
- Не создавать новые компоненты без необходимости
- Следовать существующему стилю (Tailwind classes)

### Ограничения
- НЕ трогать Sidebar без явного разрешения Runner
- НЕ трогать глобальный layout без явного разрешения Runner
- НЕ добавлять новые библиотеки без разрешения

---

## 🎨 Существующая дизайн-система

### Цвета
- Фон: `bg-gray-50` (основной), `bg-white` (карточки)
- Текст: `text-gray-900` (заголовки), `text-gray-600` (вторичный)
- Акценты: `text-black`, `focus:ring-black` (фокус)
- Ошибки: `text-red-500`

### Компоненты
- Карточки: `rounded-lg bg-white p-6 shadow-sm`
- Inputs: `rounded-lg border border-gray-300 px-3 py-2 text-sm focus:border-black focus:outline-none focus:ring-1 focus:ring-black`
- Labels: `text-sm font-medium text-gray-700`

### Spacing
- Компактный: `p-4`, `gap-4`, `space-y-4`
- Стандартный: `p-6`, `gap-6`, `space-y-6`

---

## 📁 Файлы Directory системы

- `app/directory/page.tsx` - List page
- `app/directory/new/page.tsx` - New record page
- `app/directory/[id]/page.tsx` - Detail page
- `components/DirectoryForm.tsx` - Main form component

---

**Это документ требований, а не задача для назначения.**

