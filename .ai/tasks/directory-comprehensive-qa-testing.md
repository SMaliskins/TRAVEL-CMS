# Directory Comprehensive QA Testing - Multiple Issues
**Priority:** HIGH  
**Type:** Testing & Bug Fix  
**Assigned to:** CODE WRITER + QA  
**Status:** TODO

---

## 🔍 ОБЗОР ПРОБЛЕМ

Пользователь сообщил о нескольких проблемах:
1. **Quick Search в TopBar не работает правильно** - "Tez" не находит "TEZ TOUR", но "test" находит записи
2. Необходима полная проверка `/directory` на все возможные сценарии
3. Необходима полная проверка `/directory/new` на все возможные сценарии

---

## 🔍 ПРОБЛЕМА 1: Quick Search не находит "TEZ TOUR" по запросу "Tez"

### Симптомы:
- Поиск "Tez" → не находит "TEZ TOUR"
- Поиск "TEZ" → не находит "TEZ TOUR"  
- Поиск "test" → находит "TEZ TOUR" и другие записи (4 записи)

### API Тестирование:
```bash
# Не работает
curl "http://localhost:3000/api/directory?search=Tez"  # → []
curl "http://localhost:3000/api/directory?search=TEZ"  # → []
curl "http://localhost:3000/api/directory?search=tez"  # → []

# Работает
curl "http://localhost:3000/api/directory?search=test" # → 4 записи, включая "TEZ TOUR"
```

### Причина (предположительно):

**Файл:** `app/api/directory/route.ts` (строка 134)

```typescript
query = query.or(
  `display_name.ilike.%${search}%,email.ilike.%${search}%,phone.ilike.%${search}%`
);
```

**Проблема 1:** Поиск работает только по `display_name`, `email`, `phone`
- Для Company записей `display_name` может не совпадать с `company_name`
- Нужно искать также по `company_name` (из таблицы `party_company`)

**Проблема 2:** `ilike` с `%value%` должен работать case-insensitive, но возможно:
- Проблема с форматом запроса Supabase
- Нужно проверить, как работает `.or()` с `ilike` в Supabase

**Проблема 3:** Если "test" находит "TEZ TOUR", возможно:
- "TEZ TOUR" содержит "test" в email/phone, но не в display_name
- Или проблема с токенизацией поиска

### Исправление (предварительное):

Нужно проверить:
1. Что возвращает API для записи "TEZ TOUR" - какой `display_name`?
2. Как работает поиск по `company_name` - нужно добавить в запрос?
3. Проверить формат запроса Supabase `.or()` с `ilike`

---

## 📋 ЗАДАЧИ ДЛЯ QA ТЕСТИРОВАНИЯ

### 1. Тестирование `/directory` (Список записей)

#### 1.1 Базовая функциональность:
- [ ] Страница загружается без ошибок
- [ ] Список записей отображается корректно
- [ ] Кнопка "New Record" работает (переходит на `/directory/new`)
- [ ] Клик по строке таблицы открывает детальную страницу

#### 1.2 Отображение данных:
- [ ] Колонки отображаются: Name, Type, Roles, Email, Phone
- [ ] Name отображается корректно (Person: firstName + lastName, Company: companyName)
- [ ] Type отображается корректно (person/company)
- [ ] Roles отображаются как badges (client, supplier, subagent)
- [ ] Email отображается корректно или "-" если пусто
- [ ] Phone отображается корректно или "-" если пусто

#### 1.3 Состояния:
- [ ] Loading состояние отображается при загрузке
- [ ] "No records found" отображается если нет записей
- [ ] Ошибки отображаются в консоли (не блокируют UI)

#### 1.4 Фильтры (DirectorySearchPopover):
- [ ] Quick Search работает (Name поле)
- [ ] Personal Code фильтр работает
- [ ] Phone фильтр работает
- [ ] Email фильтр работает
- [ ] Type фильтр работает (all/person/company)
- [ ] Role фильтр работает (all/client/supplier/subagent)
- [ ] Status фильтр работает (all/active/inactive)

### 2. Тестирование `/directory/new` (Создание записи)

#### 2.1 Базовая функциональность:
- [ ] Страница загружается без ошибок
- [ ] Форма отображается корректно
- [ ] Кнопки работают: Cancel, Save, Save & Close

#### 2.2 Поля формы (Person):
- [ ] Type: Person/Company radio buttons работают
- [ ] Roles: Client, Supplier, Subagent checkboxes работают
- [ ] First Name - обязательное поле, валидация работает
- [ ] Last Name - обязательное поле, валидация работает
- [ ] Personal Code - опциональное поле
- [ ] Date of Birth - опциональное поле, календарь работает
- [ ] Email - опциональное поле
- [ ] Phone - опциональное поле

#### 2.3 Поля формы (Company):
- [ ] Type: Company выбран
- [ ] Company Name - обязательное поле, валидация работает
- [ ] REG NR - опциональное поле
- [ ] ADDRESS - опциональное поле
- [ ] ACTUAL ADDRESS - опциональное поле
- [ ] Email - опциональное поле
- [ ] Phone - опциональное поле

#### 2.4 Валидация:
- [ ] Сохранение без ролей → ошибка "At least one role must be selected"
- [ ] Сохранение Person без First Name → ошибка валидации
- [ ] Сохранение Person без Last Name → ошибка валидации
- [ ] Сохранение Company без Company Name → ошибка валидации
- [ ] Сохранение с валидными данными → работает

#### 2.5 Роли и секции:
- [ ] Выбор Client role → показывается Client Type (Person/Company)
- [ ] Выбор Supplier role → показывается Supplier Details секция
- [ ] Выбор Subagent role → показывается Subagent Details секция
- [ ] Несколько ролей можно выбрать одновременно

#### 2.6 Кнопки и навигация:
- [ ] Cancel → показывает confirm modal если есть изменения
- [ ] Cancel → возвращает на `/directory` если нет изменений
- [ ] Save → сохраняет и остается на странице (redirect to edit page)
- [ ] Save & Close → сохраняет и возвращает на `/directory`
- [ ] Ctrl+S / Cmd+S → вызывает Save
- [ ] Ctrl+Enter / Cmd+Enter → вызывает Save & Close
- [ ] Escape → вызывает Cancel (если есть изменения)

#### 2.7 Состояния:
- [ ] "Unsaved changes" badge появляется при изменении полей
- [ ] Кнопки Save disabled когда форма невалидна
- [ ] Loading состояние при сохранении
- [ ] Success сообщение после сохранения
- [ ] Error сообщение при ошибке сохранения

### 3. Тестирование Quick Search в TopBar

#### 3.1 Базовая функциональность:
- [ ] Поиск открывается (Cmd+K / Ctrl+K)
- [ ] Quick Search поле отображается
- [ ] Ввод текста обновляет результаты в реальном времени
- [ ] Результаты отображаются в списке `/directory`

#### 3.2 Поиск по имени:
- [ ] Поиск "Tez" → должен найти "TEZ TOUR" ✅ (СЕЙЧАС НЕ РАБОТАЕТ)
- [ ] Поиск "TEZ" → должен найти "TEZ TOUR" ✅ (СЕЙЧАС НЕ РАБОТАЕТ)
- [ ] Поиск "test" → находит записи (работает)
- [ ] Поиск по First Name → находит Person записи
- [ ] Поиск по Last Name → находит Person записи
- [ ] Поиск по Company Name → находит Company записи

#### 3.3 Case sensitivity:
- [ ] Поиск должен быть case-insensitive
- [ ] "tez" = "Tez" = "TEZ" = "TeZ"

---

## 🔧 ИСПРАВЛЕНИЯ ДЛЯ CODE WRITER

### Исправление 1: Quick Search - добавить поиск по company_name

**Файл:** `app/api/directory/route.ts`

**Проблема:** Поиск работает только по `display_name`, но для Company записей нужно искать также по `company_name` из таблицы `party_company`.

**Решение:**
1. Проверить, как сейчас работает поиск
2. Добавить поиск по `company_name` через JOIN с `party_company`
3. Или изменить логику поиска, чтобы искать по всем полям

**Нужно проверить:**
- Какой `display_name` у записи "TEZ TOUR"?
- Работает ли поиск по `company_name`?
- Нужен ли JOIN или можно использовать подзапрос?

---

## ✅ КРИТЕРИИ ПРИЕМКИ

1. ✅ Quick Search находит "TEZ TOUR" по запросу "Tez"
2. ✅ Все кнопки на `/directory` работают корректно
3. ✅ Все поля формы на `/directory/new` работают корректно
4. ✅ Валидация работает для всех обязательных полей
5. ✅ Роли и секции отображаются корректно
6. ✅ Навигация работает (Cancel, Save, Save & Close)
7. ✅ Keyboard shortcuts работают
8. ✅ Состояния UI работают (loading, success, error)

---

## 📝 ПРИМЕЧАНИЯ

- **Важно:** Сначала исправить Quick Search (проблема с "Tez")
- **Важно:** Провести полное тестирование всех сценариев
- **Важно:** Документировать все найденные проблемы

---

**Created by:** QA Agent  
**Date:** 2025-12-25  
**Related:** Directory List, Directory New, Quick Search, TopBar

