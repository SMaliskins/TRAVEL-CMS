# QA Error Report - Directory Detail Page
**Date:** 2025-12-24  
**URL:** `http://localhost:3000/directory/d1be65fd-5222-475b-9839-df66e67ad456`  
**Branch:** `feat/directory-create`

---

## 🔍 ПРОБЛЕМЫ ВЫЯВЛЕНЫ

### ❌ ПРОБЛЕМА #1: Пустой массив ролей (roles: [])
**Severity:** CRITICAL  
**Location:** API endpoint `/api/directory/[id]/route.ts`

**Симптомы:**
- API возвращает `{"record": {"roles": []}}`
- Форма показывает ошибку валидации: "At least one role must be selected"
- Кнопки Save остаются неактивными (`disabled={isSaving || !isDirty}` работает, но форма невалидна)

**Причина:**
В функции `buildDirectoryRecord()` роли определяются на основе флагов:
```typescript
if (row.is_client) roles.push("client");
if (row.is_supplier) roles.push("supplier");
if (row.is_subagent) roles.push("subagent");
```

Эти флаги устанавливаются в GET endpoint:
```typescript
is_client: !!clientData.data,
is_supplier: !!supplierData.data,
is_subagent: !!subagentData.data,
```

**Проблема:** В базе данных для `party_id = "d1be65fd-5222-475b-9839-df66e67ad456"` нет записей в таблицах:
- `client_party`
- `partner_party`
- `subagents`

Поэтому все флаги `false`, и `roles` остается пустым массивом.

**Воспроизведение:**
1. Открыть `/directory/d1be65fd-5222-475b-9839-df66e67ad456`
2. Проверить API ответ: `curl http://localhost:3000/api/directory/d1be65fd-5222-475b-9839-df66e67ad456 | jq '.record.roles'` → `[]`
3. В интерфейсе видна ошибка: "At least one role must be selected"

**Ожидаемое поведение:**
- API должен возвращать хотя бы одну роль, если запись существует и имеет статус "active"
- Или форма должна разрешать редактирование записей без ролей (но это противоречит бизнес-логике)

**Вероятный файл ответственный:**
- `app/api/directory/[id]/route.ts` (строки 99-143)

---

### ⚠️ ПРОБЛЕМА #2: Дата рождения в будущем
**Severity:** MINOR  
**Location:** `components/DirectoryForm.tsx` (date formatting/validation)

**Симптомы:**
- Дата рождения отображается как "22.02.2026" (будущее)
- Дата в базе: `"dob":"2026-02-22"`

**Причина:**
Либо некорректные данные в БД, либо проблема с форматированием/валидацией даты.

**Воспроизведение:**
1. Открыть `/directory/d1be65fd-5222-475b-9839-df66e67ad456`
2. Проверить поле "Date of birth" → показывает "22.02.2026"

**Ожидаемое поведение:**
- Дата должна быть в прошлом (для даты рождения)
- Должна быть валидация, предотвращающая ввод дат в будущем

**Вероятный файл ответственный:**
- `components/DirectoryForm.tsx` (date input validation)
- `lib/utils/dateFormatter.ts`

---

### ⚠️ ПРОБЛЕМА #3: Форма показывает ошибку валидации при загрузке существующей записи
**Severity:** MAJOR  
**Location:** `components/DirectoryForm.tsx` (validation logic)

**Симптомы:**
- При загрузке записи с `roles: []` форма сразу показывает ошибку
- Это происходит потому что `roles.length === 0` в `validateForm()`

**Код:**
```typescript
// components/DirectoryForm.tsx:410
if (roles.length === 0) return false;
```

**Проблема:** Валидация срабатывает даже для существующих записей, которые могут не иметь ролей (некорректное состояние БД).

**Ожидаемое поведение:**
- Форма должна разрешать просмотр/редактирование даже если ролей нет
- Или должна быть возможность добавить роль при редактировании

---

## 📊 СТАТУС API ЗАПРОСОВ

**GET `/api/directory/d1be65fd-5222-475b-9839-df66e67ad456`:**
```json
{
  "record": {
    "id": "d1be65fd-5222-475b-9839-df66e67ad456",
    "display_name": "Sergejs M",
    "party_type": "person",
    "roles": [],  // ❌ ПУСТОЙ МАССИВ
    "status": "active",
    "company_id": "ca0143be-0696-4422-b949-4f4119adef36",
    "title": "Mr",
    "first_name": "Sergejs",
    "last_name": "Mališkins",
    "dob": "2026-02-22",  // ⚠️ БУДУЩЕЕ
    "personal_code": "250678-12603",
    "citizenship": "Latvia",
    "address": "Turaidas 17-53"
  }
}
```

**GET `/api/directory/d1be65fd-5222-475b-9839-df66e67ad456/statistics`:**
- Статус: 200 OK
- Данные: `{"total_spent": 0, "debt": 0, "last_trip_date": null, "next_trip_date": null, "order_count": 0}`

---

## 🔧 РЕКОМЕНДАЦИИ ПО ИСПРАВЛЕНИЮ

### 1. КРИТИЧЕСКИЙ ПРИОРИТЕТ: Исправить пустой массив ролей

**Вариант A: Добавить роль в БД (рекомендуется)**
- Выполнить SQL запрос для добавления записи в `client_party` для этого `party_id`
- Это восстановит корректное состояние данных

**Вариант B: Улучшить обработку в API**
- Если запись существует, но ролей нет, можно логировать предупреждение
- Или возвращать ошибку, если ролей нет

**SQL для исправления:**
```sql
-- Проверить наличие записей
SELECT * FROM client_party WHERE party_id = 'd1be65fd-5222-475b-9839-df66e67ad456';
SELECT * FROM partner_party WHERE party_id = 'd1be65fd-5222-475b-9839-df66e67ad456';
SELECT * FROM subagents WHERE party_id = 'd1be65fd-5222-475b-9839-df66e67ad456';

-- Добавить роль client (если нужно)
INSERT INTO client_party (party_id) VALUES ('d1be65fd-5222-475b-9839-df66e67ad456')
ON CONFLICT (party_id) DO NOTHING;
```

### 2. СРЕДНИЙ ПРИОРИТЕТ: Валидация даты рождения

- Добавить проверку, что дата рождения не в будущем
- Исправить некорректную дату в БД (если это ошибка)

### 3. НИЗКИЙ ПРИОРИТЕТ: Улучшить UX валидации

- Не показывать ошибку валидации сразу при загрузке, если запись уже существует
- Показывать ошибку только после попытки сохранения

---

## ✅ ПОДТВЕРЖДЕННЫЕ РАБОТАЮЩИЕ ФУНКЦИИ

- ✅ Страница загружается без ошибок JavaScript
- ✅ API endpoint возвращает данные (хотя и с пустыми ролями)
- ✅ Форма отображает все поля корректно
- ✅ Статистика загружается (0 заказов)
- ✅ Навигация работает
- ✅ Состояние loading корректно обрабатывается

---

## 📝 СЛЕДУЮЩИЕ ШАГИ

1. **DB Specialist:** Проверить состояние таблиц ролей в БД для этого `party_id`
2. **CODE WRITER:** Исправить обработку пустых ролей в API или форме (выбрать стратегию)
3. **QA:** Повторить тестирование после исправления

---

**Report Type:** Error Investigation  
**Priority:** HIGH (критическая проблема с ролями блокирует функциональность)





